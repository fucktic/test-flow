使用 Tailwind CSS 时，必须优先使用内置的间距比例（Spacing Scale，如 `min-w-50`），禁止使用不必要的任意值（Arbitrary values，如 `min-w-[200px]`）。
使用 Tailwind CSS 的重要标记时，统一使用后缀写法（如 `w-3!`、`bg-[#4A4542]!`），禁止前缀写法（如 `!w-3`、`!bg-[#4A4542]`）。
The class `bg-gradient-to-br` can be written as `bg-linear-to-br`
